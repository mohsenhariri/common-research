To use your cache dir instead of your home dir, add the following to your ide settings.json:

```json
{
    "remote.SSH.serverInstallPath": {
        "hpc_connection_name": "/your/cache/dir"
    }
}
```
